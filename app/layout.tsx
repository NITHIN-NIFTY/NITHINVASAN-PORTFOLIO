import { Analytics } from '@vercel/analytics/next'
import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import './globals.css'

const geistSans = Geist({ variable: '--font-geist-sans', subsets: ['latin'] })
const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
})

export const metadata: Metadata = {
  title: 'Nithin Vasan | Mechatronics Engineer',
  description: 'Portfolio of Nithin Vasan - Upcoming Mechatronics Engineer specializing in Automation, AI, Robotics, and Smart Manufacturing. Building Intelligent Machines for the Future.',
  keywords: ['Mechatronics Engineer', 'Automation', 'AI', 'Robotics', 'Smart Manufacturing', 'Industry 4.0', 'Nithin Vasan'],
  authors: [{ name: 'Nithin Vasan' }],
  openGraph: {
    title: 'Nithin Vasan | Mechatronics Engineer',
    description: 'Building Intelligent Machines for the Future',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} bg-background`}>
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
